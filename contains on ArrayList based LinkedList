public boolean<T data>{

    //check for valid data
    if(data == null)
        return false;

    ArrayListElement<T> first = getFirst();

    //iterate through the arrayList main nodes
    for(ArrayListElement<T> el = first; el != null; el = el.getNext()){

        int n = el.getN();

        //check case whereby no object in the array
        if(n == 0)
            continue;

        //iterate through the array
        Listobject<T>[] listobject = el.getData();

        for(int i = 0; i<n; i++){

            //return true if it contains same key as data
            if(listobject[i].getData() == data){
                return true;
                }

        }
    }
        return false;
}





